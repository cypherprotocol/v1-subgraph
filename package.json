{
  "name": "cypher",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cypher",
    "create-local": "graph create --node http://localhost:8020/ cypher",
    "remove-local": "graph remove --node http://localhost:8020/ cypher",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cypher"
  },
  "dependencies": {
    "@ethersproject/keccak256": "^5.7.0",
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
